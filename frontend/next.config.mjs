/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["conteo-1.onrender.com", "localhost"], // Ajoutez ici le domaine de votre serveur backend
  },
};

export default nextConfig;
